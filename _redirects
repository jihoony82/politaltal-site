# Cloudflare Workers + Static Assets _redirects
# 상대 경로만 지원 (절대 URL 불가)
#
# 도메인 간 리다이렉트(정치탈탈.com → politaltal.com)는
# Cloudflare 대시보드 → 정치탈탈.com → Rules → Redirect Rules 에서 설정
#
# 현재는 같은 도메인 내 리다이렉트가 없으므로 비어있음
