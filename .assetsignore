# Cloudflare Workers 정적 자산 업로드 제외 목록 (.gitignore 문법)
# 이유: 배포는 `npx wrangler deploy`가 저장소 루트(".")를 통째로 assets로 올린다.
# 그 안의 .git/objects/pack/*.pack 이 25 MiB 한도를 넘겨 "Asset too large"로 배포가 실패했다.
# .git 은 웹 자산이 아니므로(그리고 공개 노출도 부적절) 반드시 제외한다.
.git
._*
.DS_Store
node_modules
.assetsignore
